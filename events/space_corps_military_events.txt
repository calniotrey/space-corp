namespace = space_corps_military
# reset military orders
country_event = {
	id = space_corps_military.10
	is_triggered_only = yes
	hide_window = yes
	immediate = {
		if = {
			limit = {
				exists = event_target:military_fleet
			}
			event_target:military_fleet = {
				set_military_fleet_orders = yes
			}
		}
	}
}
